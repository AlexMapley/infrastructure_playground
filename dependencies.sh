# Step 1: Make sure you have kubernetes setup as well
brew install kubernetes-cli

# Step 2: Make sure you are running `docker-desktop` and not `minikube`
# on the docker application kubernetes section